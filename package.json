{
  "name": "project-timer",
  "displayName": "Project Timer",
  "icon": "icon.png",
  "description": "Track your coding time automatically with detailed language, project and file-level insights.",
  "version": "0.5.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/KunlingLio/project-timer.git"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "publisher": "LKL",
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "analytics",
    "code time",
    "focus",
    "productivity",
    "timer",
    "time-tracking",
    "metrics"
  ],
  "extensionKind": [
    "workspace"
  ],
  "extensionDependencies": [
    "vscode.git"
  ],
  "activationEvents": [
    "workspaceContains:**/*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "project-timer.deleteAllStorage",
        "title": "Project Timer: Delete All Storage"
      },
      {
        "command": "project-timer.openStatistics",
        "title": "Project Timer: Open Statistics"
      },
      {
        "command": "project-timer.exportData",
        "title": "Project Timer: Export Data"
      },
      {
        "command": "project-timer.importData",
        "title": "Project Timer: Import Data"
      },
      {
        "command": "project-timer.renameProject",
        "title": "Project Timer: Rename Project"
      },
      {
        "command": "project-timer.disableSyncForProject",
        "title": "Project Timer: Disable Sync For Current Project"
      },
      {
        "command": "project-timer.enableSyncForProject",
        "title": "Project Timer: Enable Sync For Current Project"
      }
    ],
    "configuration": [
      {
        "title": "Project Timer: Status Bar",
        "properties": {
          "project-timer.statusBar.enabled": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "description": "Whether the status bar timer is enabled."
          },
          "project-timer.statusBar.displayPrecision": {
            "type": "string",
            "enum": [
              "second",
              "minute",
              "hour",
              "auto"
            ],
            "default": "auto",
            "order": 2,
            "description": "The precision the status bar will display. `auto` will automatically choose the precision based on the time."
          },
          "project-timer.statusBar.displayProjectName": {
            "type": "boolean",
            "default": true,
            "order": 3,
            "description": "Whether the project name should be displayed on the status bar."
          },
          "project-timer.statusBar.displayTimeMode": {
            "type": "string",
            "enum": [
              "today",
              "total",
              "both"
            ],
            "default": "total",
            "order": 4,
            "description": "Whether today's time and total time should be displayed on the status bar."
          }
        }
      },
      {
        "title": "Project Timer: Timer",
        "properties": {
          "project-timer.timer.pauseWhenUnfocused": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "description": "Whether the timer should pause when the VSCode window is unfocused."
          },
          "project-timer.timer.unfocusedThreshold": {
            "type": "number",
            "default": 5,
            "order": 2,
            "description": "Threshold (minutes) for unfocused pause. If the window is unfocused for longer than this threshold, the timer will pause."
          },
          "project-timer.timer.pauseWhenIdle": {
            "type": "boolean",
            "default": true,
            "order": 3,
            "description": "Whether the timer should pause when user is idle."
          },
          "project-timer.timer.idleThreshold": {
            "type": "number",
            "default": 5,
            "order": 4,
            "description": "Idle time threshold (minutes). If the user is idle for longer than this threshold, the timer will pause."
          }
        }
      },
      {
        "title": "Project Timer: Synchronization",
        "properties": {
          "project-timer.synchronization.enabled": {
            "type": "boolean",
            "default": false,
            "order": 1,
            "description": "Whether synchronization is enabled."
          },
          "project-timer.synchronization.syncedProjects": {
            "type": "object",
            "default": {},
            "order": 2,
            "description": "The list of projects that are tracked for synchronization. We do not recommend you to modify this manually. You can modified this by `Project Timer: Disable Sync For Current Project` and `Project Timer: Enable Sync For Current Project` commands."
          }
        }
      },
      {
        "title": "Project Timer: Multi-Root Workspace",
        "properties": {
          "project-timer.multiRootWorkspace.warningMessage.enable": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "description": "Whether show warning message when opening a multi-root workspace."
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "build": "npx rimraf ./out && npx shx cp ./node_modules/echarts/dist/echarts.min.js ./src/view/ && npx vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.75.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "echarts": "^6.0.0",
    "eslint": "^9.39.2",
    "rimraf": "^6.1.3",
    "shx": "^0.4.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.54.0",
    "@vscode/vsce": "^3.7.1"
  }
}